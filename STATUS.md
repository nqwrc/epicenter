# status

state: active
remote: github-public
updated: 2026-09-08
stale-after-days: 30

## kpi
| kpi | target | current | as-of |
|---|---|---|---|
| fork-pr-open | 0 | 1 (nqwrc/epicenter#1, 100 commits, +16330 -2054, base main) | 2026-09-07 |
| upstream-prs-by-nqwrc | 1 | 3 open, 0 merged (EpicenterHQ/epicenter#2498, #2499, #2502) | 2026-09-08 |

## focus
- Fork of EpicenterHQ/epicenter (remote `origin`, pointed at the current URL on 2026-09-08: the org renamed from `epicenter-md`, and GitHub redirects the clone URL but the search API rejects the old name). Work on `feature/whispering-snippets` pushed to `fork` (nqwrc/epicenter): Windows dictation finishes a round trip, then the recording pill, snippets, voice commands and settings become portable; imported recipes and app rules are treated as content, not directives, and Polish drops the words the speaker did not mean to say.
- The upstream repo keeps its own AGENTS.md/CLAUDE.md (Codex owns execution, Claude is an advisory lane): this file is the harness kit only and stays out of any upstream PR.

- Three upstream PRs are open against EpicenterHQ/epicenter, all cut from `origin/main` at base 825a9954a6 and pushed to `fork` on 2026-09-08 with Nicola's word. Opened, not merged: the KPI target of 1 is met by count, the M0 evidence contract is not, since that reads on a merge. `nqwrc/epicenter#1` targets the fork's own main with 117 commits over 219 files and can never be the upstream PR - it is the holding pen the slices come out of.
  - #2498 `feature/windows-tauri-build` (2 commits, 4 files, +45): the host does not compile on Windows and its tests cannot start. Reproduced from clean `origin/main` in order - `tauri-build` panics on the missing `icons/icon.ico`, then `E0599` on the macOS-only `RunEvent::Reopen` at `lib.rs:822`, then `cargo test --lib` links but dies at `STATUS_ENTRYPOINT_NOT_FOUND` because cargo's test executables carry no manifest and bind comctl32 v5.82. Verified on origin/main, not on the working branch: all 8 workflow jobs in `.github/workflows` run Linux, none asks for a Windows or macOS runner, and `auto.release.yml` is held behind `if: false` until v8.0.0. After the branch, `cargo check` is clean and the suite runs. The number in the PR body was wrong and is corrected in a comment there: 110 passed, 8 failed, not 109/9 - the worktree had never had `bun install`, and `app_data`'s equality test shells out to `bun -e` for the TypeScript answer, so one failure was a missing module rather than a Windows gap.
  - #2499 `feature/windows-cpu-fallback` (1 commit, 1 file, +10 -27): addresses upstream #840, open since 2025-09-26 and labelled bug/windows/Critical. `load_gguf_model` named a backend per target, and naming one is a hard requirement - an explicit `Backend::Vulkan` on a machine without the runtime returns `TRANSCRIBE_ERR_BACKEND` instead of degrading. `Backend::Auto` is documented as "CPU is the always-present fallback" and is already what `bench/backend-latency` uses. No `Closes #840`: the reporter is on a 7.5.0 binary describing a startup crash, and the only mechanism reproduced here is the model-load path in the current tree, so the issue is left for the maintainer to judge. `cargo check` on this branch alone cannot run on Windows - it has no `icon.ico` and an unguarded `Reopen` - so the clean check was measured with #2498 applied, and the PR body claims nothing else.
  - #2502 `feature/windows-data-root` (2 commits, 2 files, +87 -12): the Rust half of `96c249072d`, cut from the current `origin/main` (ecba0242f6) after checking both files are byte-identical to the commit's own base. `sync_file` opened the staged blob read-only and called `sync_all`, which is `FlushFileBuffers` on Windows and refuses a read-only handle with `ERROR_ACCESS_DENIED`, so no native recording ever saved; and `app_data` mirrored the TypeScript authority's result on one config instead of its inputs, using Tauri's `app_data_dir()`, so a dev build wrote to a `blobs/` the sidecar never served. Measured in the wt-win tree with #2498 applied: 110 passed / 8 failed before, 118 passed / 1 failed after - 7 recorder tests fixed and the new dev-identifier test passing. The JS half of that commit (`@tauri-apps/plugin-http` 2.5.4 -> the crate's 2.5.9, plus `bun.lock`) is a separate IPC-protocol defect and is deliberately not in it.
  - The one test still red is `an_absolute_override_wins_without_touching_the_platform_root`, and it is a third defect, not slice 3's: Rust's `Path::is_absolute` wants a drive or UNC prefix on Windows where Node's `isAbsolute` accepts a bare rooted path, so the two implementations disagree about `\data\epicenter` and the equality this module exists to hold does not hold there. Which side moves is the authority's call; named in #2502, not fixed.

## next
- Rebrand: `feature/tironian-rebrand` (e9c99cc271), cut from `feature/whispering-snippets` and never merged back into it, so the upstream slices stay clean. The dictation app is now **Tironian**, after Tiro's shorthand (63 BC). Chosen after checking that Wispr AI filed `FLOW` and `FLOW VOICE` on 2024-08-03, so anything in that family walks into a live mark; `tironian.app`, `tironian.io` and npm `tironian` were free on 2026-09-08 and no software mark on the term was found. Alternates kept in the brand doc: Verba, Notae.
  - Tiered on measured cost, not taste. Tier 1 (executed): 23 files, every string a person or a distributor reads. Tier 2 (deliberately not executed): 520 identifiers, `WhisperingApp` 277 times and `$lib/whispering` 106, in a fork 497 commits behind upstream whose trial `git merge-tree` already conflicts on 40 files. Renaming them makes the fork unmergeable.
  - The name now has one home, `apps/whispering/src/lib/constants/brand.ts`; titles read it through `pageTitle()`, prose keeps the literal.
  - AGPL: `NOTICE` marks this a modified version of Whispering/Epicenter, names the fork point `3ef2103a72` (2026-08-27), and states that neither upstream mark is used as a product name. README credits upstream above the fold.
  - `docs/brand/tironian.md` carries the full system (mark U+204A, palette, type, voice) and the claims ledger: what the build supports today, what is roadmap, and the one weak default. No transcription default was touched. They were checked, not assumed: `transcriptionService` already defaults to `local`, `polishEnabled` to `true`, `analyticsEnabled` to `false`. `transcriptionPrompt` stays empty pending measurement.
  - Verified: 317 tests pass, `svelte-check` 0 errors on both tsconfigs, `check:doc-paths` resolves all 448 docs. `check:doc-hygiene` still fails on pre-existing stale upstream ADRs, none of them added here.
  - Pushed to `fork` (nqwrc/epicenter) at fd3d980e46. No PR opened: the branch is a fork-local product decision and has nothing to send upstream, since a rename is the one change EpicenterHQ can never take.
  - Palette and type replaced from the Vivavoce design assets (d6991b4fed). The rubrication palette carried a stated risk of landing on the most common generated-design look; the assets answer it with a dark warm-neutral system. Values read off the artboards with roles taken from application: seven-step ground stack (backgrounds only, `#2A2825` the most used value at 72), six-tone text ramp (colors only, `#8F8A82` at 114), `#D97757` as the single accent (22 fills, 9 strokes), `#7DA878` for success. Type: Instrument Sans UI, IBM Plex Mono utility, Newsreader Italic as an accent line. Light mode is unfinished in the assets and is named as unresolved rather than invented. Flagged: `#D97757` is Anthropic's own clay accent, defensible but worth choosing rather than inheriting.
  - The seven screens are NOT implemented. Three retrieval routes failed: DesignSync needs design-system authorization, bulk return through the browser MCP hits its redaction filter, and blob downloads are blocked. Unblocking needs `/design-login` run once from an interactive `claude` terminal on this machine; this desktop session cannot. Tokens, typefaces and the section inventory were extracted structurally and are already applied.
  - Two decisions open on those screens, recorded so they are not made by default: the assets carry the wordmark **Vivavoce** and it reaches delivered copy ("Apri vivavoce", "In vivavoce") while the app is Tironian; and the assets are in Italian while the app's UI is English with no i18n layer.
  - Decisions taken (2026-09-08, both files now local): name stays **Tironian**, English stays the source language, mark stays U+204A rather than the assets' waveform, tokens live in the app's own stylesheet rather than the MIT toolkit, and the accent is a new `--voce` token rather than shadcn's `--accent`, which means "hover surface" in this component library.
  - Token system applied (d2588ed138) in `apps/whispering/src/app.css`: warm graphite hue 65-80 over both themes, `--voce` driving `--ring`, Instrument Sans and IBM Plex Mono bundled through fontsource rather than fetched from Google. Verified by build artifact: production build succeeds, 32 font files emitted, emitted CSS carries `--voce` after the shared theme. Known cost: `packages/ui` still imports Geist, so a family ships unused; removing it is a toolkit change.
  - i18n layer added (d0c3ec691f) with Paraglide, compiled at build time, strategy `localStorage` then `baseLocale` and deliberately no `url`. `interfaceLocale` in the workspace is the authority and syncs; localStorage is the cache that can be read during first paint, the same shape `app.html` already uses for the theme. A disagreement reloads rather than re-renders, decided by the pure `localeAction`, which is tested. Converted as a vertical slice: two voice-command errors, settings description, boot-failure title, and a language selector. Roughly 500 strings remain and are deliberately untouched.
  - Verified: 323 tests pass, `svelte-check` 0 errors on both tsconfigs, production build succeeds, both English and Italian strings present in the emitted bundle.
  - Still not implemented: the thirteen artboards as screens. No browser render path exists (`#platform/os` resolves unconditionally to the Tauri implementation), so a layout rewrite could not be looked at. Same gate as the remaining string extraction.
  - String extraction done (3a040b0b5b): 484 messages across 82 files, up from 27. Built around one guarantee, that every English message is the exact text it replaced, so `en` renders unchanged and typecheck/suite/build are real evidence. Three defects caught in flight rather than shipped: `label` matching the tail of `aria-label` and winning the right-to-left pass (would have written `aria-{m.key()}` into markup, caught by the dry run as ten overlaps); thirteen messages baking the product name in, undoing the single-constant rule the rebrand established; eleven messages with no call site. All three classes are now suite invariants that need no build and no browser. `typecheck` compiles the catalogue first, since the Paraglide compiler lives in the Vite plugin and a standalone typecheck was reading a stale catalogue.
  - Verified: 327 tests, `svelte-check` 0 errors on both tsconfigs, production build succeeds, 478 of 483 messages present verbatim in the emitted bundle. The five absent are the dev-only Debug page and one unmounted component, both confirmed by reading call sites.
  - Remaining i18n work, named rather than implied: Italian falls back per message for the 460 new keys (Paraglide compiles a missing translation as a direct alias to the English function), and 35 interpolated strings stay literal because each needs its own placeholder decided.
- Watch #2498, #2499 and #2502 for maintainer review; none has CI that can run it, since no upstream workflow builds on Windows. A merge, not the opening, is what M0 reads as evidence. The three are file-disjoint and merge in any order; only checking them on Windows needs #2498 first.
- #848 (tray, 18 reactions, top of the tracker) needed no code, and that is the finding: the feature is on `origin/main` and has been since 003845a9d6 (2026-07-10). `shell.rs` builds the tray with its menu and swaps the icon while recording; `lib.rs` answers every built-in window's `CloseRequested` with `prevent_close()` + `hide()`, with no `cfg`, so Windows runs the same path. It is unreleased: the newest published release is Whispering v7.11.0 (2025-12-27), seven months earlier, and `auto.release.yml` sits behind `if: false` until v8.0.0 is tagged. Posted that on the issue. The `[x]` todos in its body are the reporting user's, not a maintainer's status, which is what made it read as unbuilt - and the tell was that the last user comment (2026-04-24) predates the implementation. Gap-mining step 3 answered by reading the tree and the release dates, which is exactly the step a scanner cannot do.
- Decide whether the branch goes upstream as one PR or is split (platform fixes first, then pill, snippets, commands, settings); rebase on origin/main before either. Measured 2026-09-08 (the clone was shallow until then, so earlier divergence numbers were graft artifacts): fork point 3ef2103a72 on 2026-08-27, upstream 497 commits ahead, 78 of them breaking. A trial `git merge-tree` conflicts on 40 files, of which 24 are free because our only edit there is the formatter commit ee777c80ae; the cost is not the markers but that workspace/index.ts and the rest of our store code face a rewritten data vocabulary. Deliberately not merged: 37 upstream commits touch apps/whispering and nearly all are refactor(data)! dragging it along as a consumer. Exactly one is a fix(whispering), and it repairs a hazard upstream introduced in the same window. Merge, not rebase, when it happens: a rebase rewrites 119 commits and needs a force push.
- `transcriptionPrompt` defaults to empty (app.ts:138) and is the only disfluency lever that works with Polish off, which is the fresh-install state. A clean-prose default biases the first pass; it is provider-dependent and interacts with the dictionary matcher ADR-0099 defers, so it needs measuring before it is changed.
- .claude/settings.local.json is tracked upstream but holds machine-local auto-mode config, so it carries a local skip-worktree flag: the tree reads clean and the flag shows up in `git ls-files -v` if a rebase ever trips on it. Asking upstream to untrack it is still the durable fix; .sentry-native/ is gitignored.

## blockers
- none
